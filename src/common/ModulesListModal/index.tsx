import { Course } from '@/components/Materials/utils/interfaces';

interface Props {
  list?: Course[];
  close: () => void;
}

export default function ModulesListModal({ list, close }: Props) {
  return <div></div>;
}
